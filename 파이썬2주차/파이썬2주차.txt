파이썬 프로그래밍 2주차

인터프리트 방식
- 동시 통역처럼, 프로그램의 코드가 한줄씩 순서대로 해석되고 실행
- 인터프리터(interpreter)
    한 줄 한 줄의 해석을 담당하는 프로그램

컴파일 방식
 - 컴파일러(compiler)
    컴파일을 담당하는 개발 도구 소프트웨어

자료의 종류

글자, 숫자, 날짜 등
- 일상 생활에서 사용되는 자료들

문자열(string)
- 일련(sequence)의 문자(character) 모임
- 작은따옴표나 큰따옴표로 앞뒤를 둘러싸 표현

문자열 표현
따옴표로 둘러싸면 모두 문자열
함수 print(출력될 자료)
- 문자열이나 숫자 등의 자료를 콘솔에 출력하는 일을 수행
- 출력 이후에 다음줄로 이동해 출력을 준비

>>> print( "Hello string! ' )>>구문오류 앞뒤 따옴표가 나오면 오류가뜸

문자열 연산자 +, * 와 주석

+
- 문자열에서 문자열을 연결(concatenation)

*
- 문자열에서 문자열을 지정된 수만큼 반복
- 교환법칙 성립, 앞뒤에 중에 하나는 반복 수

삼중 따옴표와 주석
삼중 따옴표 : 여러 줄에 걸쳐 문자열을 처리(여러줄 주석 가능)
주석 : 소스 설명 >> #~~~~~~라인끝까지 한줄만가능 여러줄 불가능

연산자
/ 몫 >> 둘 다 정수여도 실수가 나옴
//  몫 나누기 >> 정수가 나옴
** 거듭제곱 >> 2**3 >> 2의 3승

최근 연산 결과 저장소 _와 연산자 % //
_(언더바)
  - 대화형 모드에서 마지막에 실행된 결괏값은 특별한 저장 공간인 _에 저장됨

한줄에 여러 자료 출력과 함수 eval()

콤마로 구분하여 출력


표현식 문자열 실행 함수 eval()
eval : 문자열을사용해 계산이가능함
ex : eval('1+4'), eval(' "java "*3 ')

자료형과 type()함수

자료형 : 정수와 실수, 문자열 등
type()함수 : 자료형을 직접 알아 보는 방법

키워드 : 프로그래밍 언어가 이미 정해놓은 단어
이미 예약된 단어(reserved words)
 - 프로그래밍 언어 문법에서 사용
파이썬에서 사용하는 키워드의 개수는 총33개
키워드 import는 라이브러리 keyword를 가져와 사용한다는 의미

변수 이름을 붙일 때의 규칙
식별자(identifiers)
- 변수, 함수와 클래스 이름 등 프로그래머가 이름을 짓는 단어
- 식별자를 구성하는 문자
영문자(한글도 가능)와 숫자 그리고_사용

제한
- 문자는 대소문자의 영문자(a,b,c,A,B...),숫자(0,1,2...)그리고 _로 구성
대소문자는 구별, 한글도 가능
숫자는 맨 앞에 올 수 없다. 그러므로 영문자로 시작
import, True, False등과 같은 키워드는 사용 불가
공백문자 불가
맨앞에_도 가능하지만 특수한 사용이 아니라면 가능한 사용하지 않도록 권고
 한번에 여러 자료 대입과 함수 divmod()
 - 함수 divmod(a,b)를 사용해 지구와 달의 거리인 384,400km를
38만4,400km로 출력하는코딩
>>>28// 3, 28% 3
(9, 1)
>>>divmod(28, 3)
(9, 1)
>>>a, b = 17, 5
>>>d, m = divmod(a, b)
>>>print(d, m)
3 2

표준입력의 이해
실 생활에서의 입력
-커피 전문점의 주문 처리 과정
-프로그램 작성 과정에서 사용자의 입력을 받아 처리해야하는경우

함수 imput()사용

>>>input()
java
>>> pl = input()
python
>>>print(pl)
python

16진수, 8진수, 2진수 상수 표현 - 매우중요

16진수
- 맨 앞에 0x로 시작
8진수
- 0o
2진수
- 0b
	각각의 진수를 표시하는 알파벳은 대소문자 모두 가능

10진수를 바로 16진수, 8진수, 2진수로 표현되는 문자열로 변환

>>>bin(7), bin(16), bin(12)
('0b111', '0b10000', '0b1100')
>>>oct(8),oct(10)
>>>('0o10', '0o12')
>>>hex(14), hex(15)
('0xe', '0xf')

2-15,2-16코딩 응용해서 시험에나올수있음 매우중요

int(정수, 진법기수)

여러 진수 상수 형태 문자열의 10진수 변환
- 함수 int('strnum') 또는 int('strnum',10)
	10진수 형태의 문자열을 10진수 정수로 변환

함수 int('strnum', n)호출
N은 변환하려는 문자열 진수의 숫자를 2,8,10,16 등 사용

함수 int('strnum',0)호출
-strnum의 맨 앞이 0b, 0o, 0x에 따라
각각의 문자열을 자동으로 각각 2진수, 8진수, 16진수로 인지해
10진수로 변한

>>>int('0b11',0) # 2진수로 인식
3

 

 

